name: Micro Frontends

on:
  push:
    branches:
      - main
  pull_request:

jobs:

  shell:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Install dependencies
        working-directory: frontend/shell
        run: npm ci

      - name: Build
        working-directory: frontend/shell
        run: npm run build

      - name: Login to Docker Hub
        if: github.event_name == 'push'
        uses: docker/login-action@v5
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        if: github.event_name == 'push'
        uses: docker/build-push-action@v6
        with:
          context: ./frontend/shell
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/board-game-shell:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/board-game-shell:${{ github.sha }}

  catalog-mf:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Install dependencies
        working-directory: frontend/catalog-mf
        run: npm ci

      - name: Build
        working-directory: frontend/catalog-mf
        run: npm run build

      - name: Login to Docker Hub
        if: github.event_name == 'push'
        uses: docker/login-action@v5
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        if: github.event_name == 'push'
        uses: docker/build-push-action@v6
        with:
          context: ./frontend/catalog-mf
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/board-game-catalog-mf:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/board-game-catalog-mf:${{ github.sha }}

  user-management-mf:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Install dependencies
        working-directory: frontend/user-management-mf
        run: npm ci

      - name: Build
        working-directory: frontend/user-management-mf
        run: npm run build

      - name: Login to Docker Hub
        if: github.event_name == 'push'
        uses: docker/login-action@v5
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        if: github.event_name == 'push'
        uses: docker/build-push-action@v6
        with:
          context: ./frontend/user-management-mf
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/board-game-user-management-mf:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/board-game-user-management-mf:${{ github.sha }}

  borrowing-mf:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Install dependencies
        working-directory: frontend/borrowing-mf
        run: npm ci

      - name: Build
        working-directory: frontend/borrowing-mf
        run: npm run build

      - name: Login to Docker Hub
        if: github.event_name == 'push'
        uses: docker/login-action@v5
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        if: github.event_name == 'push'
        uses: docker/build-push-action@v6
        with:
          context: ./frontend/borrowing-mf
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/board-game-borrowing-mf:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/board-game-borrowing-mf:${{ github.sha }}